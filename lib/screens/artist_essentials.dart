import 'package:flutter/material.dart';

class ArtistEssentialsScreen extends StatelessWidget {
  const ArtistEssentialsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
