import 'package:flutter/material.dart';

class SoundsOFJamaicaScreen extends StatelessWidget {
  const SoundsOFJamaicaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
