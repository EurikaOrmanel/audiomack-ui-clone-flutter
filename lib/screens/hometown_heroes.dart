import 'package:flutter/material.dart';

class HomeTown_Heroes_Screen extends StatelessWidget {
  const HomeTown_Heroes_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
