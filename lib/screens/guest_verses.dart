import 'package:flutter/material.dart';

class GuestVersesScreen extends StatelessWidget {
  const GuestVersesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
