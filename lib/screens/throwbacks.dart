import 'package:flutter/material.dart';

class ThrowbackScreen extends StatelessWidget {
  const ThrowbackScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
