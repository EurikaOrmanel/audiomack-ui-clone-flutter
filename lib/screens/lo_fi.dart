import 'package:flutter/material.dart';

class Lo_FiScreen extends StatelessWidget {
  const Lo_FiScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
